import 'package:flutter/material.dart';

class HomeDoctorPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Página del Doctor")),
      body: Center(child: Text("Bienvenido a la página del doctor")),
    );
  }
}
